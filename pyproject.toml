[project]
name = "oxys"
version = "2.0.0"
description = "Real-Time Streaming Data Integrity & Anomaly Protection System"
readme = "README.md"
requires-python = ">=3.11"

[tool.vercel]
entrypoint = "server.main:app"
